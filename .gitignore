.DS_Store
__pycache__/
*.pyc
node_modules/
# Specimen image outputs are regenerated by render.mjs and published to the
# profile repo; keep only the auditable layout.json / audit.json in git.
profile-art/specimen/out/*.png
profile-art/specimen/out/*.svg
# Artifact fonts are fetched from Google Fonts (see artifacts/README); artifact
# outputs are regenerated and published to the profile repo.
profile-art/artifacts/fonts/
profile-art/artifacts/out/
profile-art/artifacts/_*
# Local working archive of duplicated media (read-only originals elsewhere).
# Not committed — regenerable from source paths in PORTFOLIO_CURATION_PLAN.md.
portfolio-archive/
